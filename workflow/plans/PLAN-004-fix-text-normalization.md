# PLAN-004: Исправить нормализацию текста

Этот план описывает шаги по исправлению регулярного выражения в `src/normalize.js` для сохранения дефисов и удаления избыточных символов.

## Шаги

1. [x] Создать тесты для `normalizeText` в `test/normalize.test.js`, чтобы зафиксировать текущее поведение и подтвердить проблему с дефисами.
2. [x] Обновить регулярное выражение в `src/normalize.js`.
3. [x] Запустить тесты и убедиться, что они проходят.
4. [x] Обновить статус issue `ISS-004`.

## Ожидаемый результат

- `normalizeText("1-комнатная")` возвращает `"1-комнатная"`, а не `"1 комнатная"`.
- Удален избыточный сегмент `dinдинара` из регулярного выражения.
- Все существующие тесты (если они затронуты) проходят.
